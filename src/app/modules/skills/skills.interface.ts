export interface ISkill {
    image: string;
    name: string;
    description?: string; // Optional description of the skill
    level?: string; // Optional skill proficiency level (e.g., Beginner, Intermediate, Expert)
  }
  